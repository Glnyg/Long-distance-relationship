apiVersion: v1
kind: ServiceAccount
metadata:
  name: aiapp-workload
  namespace: aiapp
  labels:
    # 先使用统一 ServiceAccount，后续生产环境可按服务拆更细的权限。
    app.kubernetes.io/name: aiapp-workload
    app.kubernetes.io/part-of: aiapp
