apiVersion: v1
kind: Service
metadata:
  name: aiapp-minio
  namespace: aiapp
  labels:
    app.kubernetes.io/name: minio
    app.kubernetes.io/part-of: aiapp
spec:
  ports:
    - name: api
      port: 9000
      targetPort: api
    - name: console
      port: 9001
      targetPort: console
  selector:
    app.kubernetes.io/name: minio
    app.kubernetes.io/part-of: aiapp
