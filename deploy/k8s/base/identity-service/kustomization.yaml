apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# IdentityService 负责账号登录和 token。Secret 示例只能使用 CHANGE_ME，真实值由环境注入。
resources:
  - secret.example.yaml
  - deployment.yaml
  - service.yaml
