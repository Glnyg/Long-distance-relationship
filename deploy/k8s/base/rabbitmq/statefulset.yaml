apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: aiapp-rabbitmq
  namespace: aiapp
  labels:
    app.kubernetes.io/name: rabbitmq
    app.kubernetes.io/part-of: aiapp
spec:
  # RabbitMQ 是有状态组件，消息队列数据需要挂载持久卷。
  serviceName: aiapp-rabbitmq
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: rabbitmq
      app.kubernetes.io/part-of: aiapp
  template:
    metadata:
      labels:
        app.kubernetes.io/name: rabbitmq
        app.kubernetes.io/part-of: aiapp
    spec:
      serviceAccountName: aiapp-workload
      containers:
        - name: rabbitmq
          # management 镜像自带 Web 管理界面，方便开发和排障。
          image: rabbitmq:3.13-management-alpine
          imagePullPolicy: IfNotPresent
          ports:
            - name: amqp
              containerPort: 5672
            - name: management
              containerPort: 15672
          envFrom:
            - secretRef:
                name: aiapp-rabbitmq-secret
          readinessProbe:
            # rabbitmq-diagnostics ping 能确认节点已经启动并可接受连接。
            exec:
              command:
                - rabbitmq-diagnostics
                - ping
            initialDelaySeconds: 20
            periodSeconds: 10
          livenessProbe:
            exec:
              command:
                - rabbitmq-diagnostics
                - ping
            initialDelaySeconds: 60
            periodSeconds: 30
          resources:
            requests:
              cpu: 250m
              memory: 512Mi
            limits:
              cpu: "1"
              memory: 1Gi
          volumeMounts:
            - name: rabbitmq-data
              # 队列元数据和持久化消息存放在这里。
              mountPath: /var/lib/rabbitmq
  volumeClaimTemplates:
    - metadata:
        name: rabbitmq-data
      spec:
        accessModes:
          - ReadWriteOnce
        resources:
          requests:
            storage: 10Gi
