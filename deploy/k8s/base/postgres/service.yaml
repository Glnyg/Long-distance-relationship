apiVersion: v1
kind: Service
metadata:
  name: aiapp-postgres
  namespace: aiapp
  labels:
    app.kubernetes.io/name: postgres
    app.kubernetes.io/part-of: aiapp
spec:
  ports:
    - name: postgres
      # 集群内部服务通过 aiapp-postgres:5432 访问数据库。
      port: 5432
      targetPort: postgres
  selector:
    app.kubernetes.io/name: postgres
    app.kubernetes.io/part-of: aiapp
