apiVersion: v1
kind: Service
metadata:
  name: aiapp-redis
  namespace: aiapp
  labels:
    app.kubernetes.io/name: redis
    app.kubernetes.io/part-of: aiapp
spec:
  ports:
    - name: redis
      # 集群内部服务通过 aiapp-redis:6379 访问 Redis。
      port: 6379
      targetPort: redis
  selector:
    app.kubernetes.io/name: redis
    app.kubernetes.io/part-of: aiapp
