apiVersion: v1
kind: Secret
metadata:
  name: aiapp-redis-secret
  namespace: aiapp
  labels:
    app.kubernetes.io/name: redis
    app.kubernetes.io/part-of: aiapp
type: Opaque
stringData:
  # 示例密码，生产环境必须替换。
  REDIS_PASSWORD: CHANGE_ME_REDIS_PASSWORD
